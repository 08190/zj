# MVP 0.5 验证记录

Render免费部署准备：112项自动检查通过；专用上传包解压到独立目录后，同样112项检查通过。新增平台域名/端口适配、最小健康检查与临时存储提示。render.yaml显式指定Free，不添加磁盘或数据库；实际Render创建、平台配置校验、外部访问和云端真实模型仍未完成。上传步骤见RENDER_FREE.md。

部署准备追加检查：108项自动检查通过（包含访问口令、未登录写入拒绝、来源检查、持久请求计数、日/小时限制与配置拒绝）。修复了SQLite连接未及时关闭导致Windows测试文件锁定的问题。examples三组已用当前代码离线重新生成。通用Docker和Hugging Face部署文件已准备，但本机无Docker；容器构建、平台TLS、外部访问及云端模型调用未验收，不存在已发布入口。见DEPLOYMENT.md与SUBMISSION.md。

本轮最终 Python unittest：99项通过（包含此前87项与新增12项复核检查）。新增复核页面检查通过：登记、接单、补充请求、报告变更过期、重新提交、记录意见、刷新恢复、丢失响应后请求编号不变、导出、窄屏无横向溢出；诊断停机要求和复机禁止保持。旧会话恢复页面回归通过。测试使用临时数据库/随机端口/模拟或离线配置，没有使用用户密钥或发起真实模型请求。

状态更新（2026-09-07）：`last-live-check.json` 已记录新版 multiturn 通过结果。普通输入的模型状态为 applied、检索为 embedding；危险输入在本地安全门阻断；读数保留、工单稳定、刷新恢复和安全锁定五项检查全部通过。正式专家身份签署仍未完成。下列保留0.4基础检查记录。

- Python unittest：87 项通过。包括原有36条状态子场景、24条修订案例子场景，以及安全作用域、会话重试、跨实例并发、引用绑定、清单完整性、工单一致性等检查。子场景不另算87项测试方法。
- 独立运行离线评测：24/24通过，详情见 acceptance-offline-result.json。这是已用于调试的工程回归集，不是真实诊断准确率。
- Edge 页面复核通过：响应丢失后刷新仍保留请求编号；未确认提交期间禁止改写和新建；旧请求重试后加载最新版本；恢复失败阻止误建；旧服务提示重启；无页面脚本错误。
- 本轮使用临时数据库与随机测试端口，未中断用户8765服务；未读取或发送用户密钥，未发起真实OpenAI请求。
- 多轮真实检查入口已加入；其内部流程通过模拟供应商测试，确认两轮普通模型调用和两轮安全阻断。模拟通过不等于真实服务验收。
- 用户此前的单轮OpenAI检查为passed（2026-09-06T17:35:32.855852+00:00）；不能据此宣称新版多轮已通过。新版需运行 check-multiturn-openai.cmd，查看 multiturn.status。

运行离线评测：python -m apx240.evaluate。默认强制关闭远程配置，不消耗API；结果保存runtime/acceptance-result.json。--live为显式远程评测，会对符合条件的案例调用模型与向量服务。

已完成：新版真实模型多轮验收。未完成：维修专家签署、真实标签准确率、生产权限隔离和飞书同步。设计边界详见 DESIGN_AUDIT.md。
